# KOY CALENDAR — PROJECT HANDOFF DOC

**Purpose:** Bring any new developer or future Claude instance up to speed on Koy in 10 minutes. Also serves as a safety backup if the current architect chat hits its limit.

**Last updated:** May 6, 2026
**Status:** Sprint 2 in progress (PR #2 active — calendar editing)

---

## 1. WHAT KOY IS (the 30-second version)

Koy is a customizable page builder where the same product serves wildly different audiences (traders, tattoo artists, working parents, teens, podcasters, etc.) via persona-tuned templates and blocks. Users get a public URL at `/u/{handle}` that becomes their personal corner of the internet. The vision includes a marketplace where users can buy/sell page layouts (skeleton-only, $8.99 floor, 15% take rate) and a "package" model ($25.99 vertical bundles like Tattoo Package, Hair Stylist Package) that get built reactively based on observed user behavior, not predicted.

**It is NOT:** a productivity tool, a Linktree clone, a calendar app, a Notion competitor. It IS: self-expression with a creator economy underneath.

---

## 2. WHO IS WHO

- **Khalid (founder, GitHub: kvalbrun25-code):** Non-coder. Architect/strategy lives in his Claude.ai chat. Token-conscious, lean communication, calls his Claude "Claudisson"/"Claudesson."
- **The architect (Claude.ai chat, this instance):** Spec writing, strategic decisions, troubleshooting, design calls. Does NOT write production code directly.
- **The implementor(s) (Claude Code on the web):** Reads specs, writes code, commits to feature branches, opens PRs. Currently 1 active; Khalid is considering adding a second.
- **GPT (separate tool):** Marketing only. Not involved in product or engineering decisions.

**Division of labor:** Architect specs → Implementor builds → Khalid tests → merge to main → Vercel auto-deploys.

---

## 3. WHERE WE ARE TODAY

### Sprint 1 — SHIPPED ✅ (merged to main)

Mobile single-column re-render. Public shareable URLs at `/u/{handle}`. Header collapse on mobile (`← ● KOY ⋯` + bottom sheet). SQL hardening (user_events insert policy, one-active-page-per-user constraint, updated_at triggers, missing DELETE policies, comment visibility mirroring). Handle-claim onboarding step. Real share URL.

### Sprint 2 — IN PROGRESS 🟡

10 chunks across 4 PRs. Status:

- **PR #1 (chunks 4 + 5 + 2 + 3): SHIPPED ✅** — Branding (green K favicon and OG image, SVG-only per Khalid's call), YouTube embed fix, login/logout control, account settings page.
- **PR #2 (chunks 6 + 7): IN PROGRESS** — Editable calendar with bottom-sheet date editor (chunk 6), freeform mini-canvas per cell (chunk 7). Currently at sub-step 6a. Khalid is doing per-sub-step pause-and-test cadence (NOT yolo mode).
- **PR #3 (chunk 8): NOT STARTED** — Image upload via Supabase Storage. Requires Khalid to manually create `user-uploads` bucket in Supabase Dashboard with public read enabled.
- **PR #4 (chunk 9): NOT STARTED** — Tenor GIF integration. Requires Khalid to get free Tenor API key from `developers.google.com/tenor` and add `VITE_TENOR_API_KEY` to Vercel env vars.
- **Chunk 10:** Final verification only (anonymous viewer can see uploaded images).

### Sprint 3 and beyond — SPECCED, NOT BUILT

- Months archive rebuild (real page snapshots instead of fake P&L data, user-nameable months, opt-in P&L aggregation, optional per-month shareable URLs).
- Schema rename `trading_days` → `calendar_entries` HARD-DROP (Sprint 2 added new table; Sprint 3 drops old).
- Tattoo Pro template (lean wedge, NOT full booking SaaS).
- Marketplace v1 (templates_marketplace table, purchase flow, Stripe Connect, designer payouts).
- Package architecture (Sprint 4+ — see section 9).

---

## 4. STACK & INFRASTRUCTURE

| Layer | Tool | Notes |
|---|---|---|
| Frontend | React + Vite | Single-file `src/App.jsx`, will eventually split |
| Backend | Supabase (Postgres + Auth + Storage + RLS) | Free tier currently |
| Hosting | Vercel | Auto-deploys `main` branch in ~60s |
| Repo | github.com/kvalbrun25-code/koy-calendar | Private |
| Live URL | https://koy-calendar-ecek.vercel.app | Real domain TBD pre-launch |

### Critical environment variables

- `VITE_SUPABASE_URL` (set, in Vercel)
- `VITE_SUPABASE_ANON_KEY` (set, in Vercel)
- `VITE_TENOR_API_KEY` (NOT YET SET — required before PR #4)

### Supabase setup that needs Khalid's hand

- Storage bucket `user-uploads` must be created in Dashboard before PR #3. Public read ON, authenticated write only. RLS policies are in `sprint-2-migration.sql`.

---

## 5. NON-NEGOTIABLE CODING CONVENTIONS

These have bitten the project before. Violating them costs hours.

### React imports

```js
// CORRECT
import { useState, useEffect } from "react"
function App() { ... }
export default App

// WRONG — produces "_react2 is not defined" build errors
import React, { useState } from "react"
export default function App() { ... }
```

The artifact renderer breaks on the wrong pattern. ALWAYS named imports from "react", ALWAYS function declarations, ALWAYS `export default App` on its own line at file bottom.

### File structure

- `src/App.jsx` — main app (single file, large, will refactor later)
- `src/main.jsx` — entry point with `<BrowserRouter>` wrapping `<App />`
- `public/` — static assets (favicon.svg, og-image.svg)
- `index.html` — root HTML, contains OG meta tags

### No new dependencies without justification

Stack is React + Vite + Supabase + react-router-dom. Don't add UI libraries (Material UI, Chakra, etc.), animation libraries (Framer Motion, etc.), or CSS frameworks (Tailwind isn't installed). Plain CSS and inline styles are the convention.

### Commit conventions

- One logical change per commit
- Format: `Sprint N (X/Y): {short description}`
- Push after every commit (stream timeout protection)
- Branch naming: `claude/sprint-{N}-pr-{P}-{theme}` e.g. `claude/sprint-2-pr-2-calendar-editing`

### Stream timeout discipline

Claude Code on the web has a known stream idle timeout bug. Mitigation:

- Commit and push after every sub-step
- Don't batch 10 changes into one massive response
- If a chunk is taking long, pause and ask Khalid for "go" between sub-steps
- If you hit "API Error: Stream idle timeout - partial response received": commit what you have if you can, tell Khalid which sub-step you stopped at, wait for restart instruction

---

## 6. DATA MODEL (current state of production)

### Tables

**profiles** — one per user, auto-created on signup
```
id (uuid, FK to auth.users), handle (unique), display_name, bio,
is_premium (bool), data_sharing (bool), created_at, updated_at
```

**pages** — saved page layouts, multiple per user, only one active
```
id, user_id (FK profiles), name, is_active (bool), page_settings (jsonb),
blocks (jsonb), created_at, updated_at
```
- UNIQUE constraint: only one `is_active=true` per `user_id` (Sprint 1 migration)

**comments** — Discord-style comments on active pages
```
id, page_id (FK pages), author_id (FK profiles), author_name,
author_avatar (default ⭐), message, created_at
```

**user_events** — the "money table" for AI training data licensing
```
id (bigint), user_id (FK profiles, nullable for guests), event_type,
event_data (jsonb), device, shareable (bool default true), created_at
```
- INSERT policy: `auth.uid() = user_id OR user_id IS NULL` (Sprint 1)

**trading_days** — LEGACY, will be dropped in Sprint 3
- Kept for backwards compat during Sprint 2

**calendar_entries** — NEW in Sprint 2, replaces trading_days
```
id, user_id (FK profiles), day, emoji, label, numeric_value, count_value,
journal, mood, freeform_blocks (jsonb), created_at, updated_at
unique(user_id, day)
```
- `freeform_blocks` format: `[{type: 'text'|'emoji'|'sticker', content, x: 0-100, y: 0-100, fontSize?, color?}, ...]`
- Coordinates are percentages (cell-size independent)
- Hard cap: 20 elements per cell, client-enforced

**month_archives** — SPECCED, NOT BUILT (Sprint 3)
```
year, month, custom_name, blocks_snapshot (jsonb),
page_settings_snapshot (jsonb), unique(user_id, year, month)
```

### Storage

- `user-uploads` bucket (Sprint 2, requires manual creation)
- Path convention: `{user_id}/{block_id}/{timestamp}-{filename}`
- Limits: 5MB free / 25MB premium, MIME types jpeg/png/webp/gif

---

## 7. KEY ARCHITECTURAL DECISIONS (the things you'd otherwise have to ask)

### Public visibility
All pages are public-with-link by default. No private/listed toggle yet. That's Sprint 4+ (tied to marketplace launch).

### Mobile rendering
Below 768px viewport = single-column vertical stack ordered by y then x of each block on desktop. Drag DISABLED on mobile entirely. Tap a block → action sheet (delete/edit/style/up/down). NO mobile-specific layouts — same blocks, different rendering.

### Templates
Copied at save-time, not referenced. Template updates do NOT propagate to existing pages. Critical for marketplace integrity (buyer's purchase is a frozen snapshot).

### Marketplace (when built)
- Sellable layouts contain **structure only** (block positions, sizes, rotations, colors, fonts, embed slots)
- NOT content (text, images, embeds, calendar entries, journal data)
- Default opt-in OFF per save (must be explicitly enabled per save)
- $8.99 floor, 15% platform fee
- Anyone can sell, every page can be sold (open supply)
- Discovery via feeds + curated badges, NOT search/categories

### Authentication
Supabase native email + password only currently. OAuth (Google/Apple) and magic links not wired. Email verification currently OFF — turn on before public launch.

### Mobile detection
`useIsMobile()` hook reading `window.matchMedia('(max-width: 767px)')`. NEVER user-agent sniffing.

### URLs
- `/` — home (start screen for visitors, own page for logged-in users)
- `/u/{handle}` — public read-only view of user's active page
- `/u/{handle}/edit` — owner-only edit view (redirects to `/` if logged out, to `/u/{handle}` if wrong user)
- `/account` — settings page (Sprint 2)

### URL format note
Current: `koy-calendar-ecek.vercel.app/u/{handle}`. Real domain TBD before public launch. Khalid's roadmap includes custom domain support as a Pro upsell (`yourname.com` → their Koy page).

---

## 8. ACTIVE OPEN QUESTIONS / DECISIONS LOG

These are decisions that have been made but might come up again:

| Question | Decision | When |
|---|---|---|
| OG image format | SVG-only, ship without PNG; swap later | Sprint 2 chunk 4 |
| Freeform palette source | Fixed 12 emoji + "more" button to full picker | Sprint 2 chunk 7 |
| Day delete UX | Single Delete button, confirmation if freeform_blocks non-empty | Sprint 2 chunk 7 |
| Default page visibility | Public-with-link (anyone with URL can view) | Sprint 1 |
| URL prefix structure | `/u/{handle}` not bare `koy.app/{handle}` (future-proofs `/m/`, `/c/`, `/p/`) | Sprint 1 |
| Save-page UX | Save = make this my live page (auto-deactivate older pages); no explicit Activate button | Sprint 1 |
| Marketplace what transfers | Skeleton only, not content/embeds/personal data | Architect strategy session |
| Marketplace contests | Free entry, winners get featured-creator status (not paid prizes — sweepstakes law risk) | Architect strategy session |
| Multi-month time machine | Real page snapshots, user-nameable months, opt-in P&L | Sprint 3 spec |

---

## 9. NEXT-NEXT BACKLOG (Sprint 4+, NOT YET BUILT)

Khalid wants these in the order shown after Sprint 2 + 3 ship:

1. **Package architecture** (Sprint 4) — `packages` table, `user_packages` join table, package-gated features and blocks. Build the *capability* without committing to a specific vertical. ~$25.99 hybrid pricing (one-time for templates/content, recurring features tied to active subscription).
2. **First package** (Sprint 5) — chosen by data after 90 days of real users. Could be tattoo, hair, working parents, podcasters — whichever persona shows up most.
3. **Marketplace v1** (Sprint 6) — Stripe Connect integration, designer payouts, "Use this layout" purchase flow on every public page, $8.99 floor, 15% take.
4. **Custom domains** (Sprint 7) — Pro-tier upsell, vercel.app/u/handle → yourname.com.
5. **Discovery feeds** (Sprint 8) — algorithmic + curated, "Pages you might like" / "Trending layouts" / "Top this week."
6. **Contest mechanics** (Sprint 9) — bi-weekly free contests, winners featured in marketplace.
7. **Months archive — phase 2** — per-month shareable URLs (`/u/handle/october-2026`).
8. **OAuth providers** — Google + Apple sign-in.
9. **Email verification** — turn on before public launch.

---

## 10. THINGS A NEW DEVELOPER NEEDS TO DO BEFORE WRITING CODE

If you're a second developer joining the project (or a future Claude instance recovering from a chat limit):

1. **Read this entire document.**
2. **Check the latest commits on the repo** to see what's actually been shipped vs. specced.
3. **Read `src/App.jsx` end to end** before any change. The file is large but structured.
4. **Check the active branches** to see what's in flight: `git branch -a` or look at GitHub.
5. **Confirm with Khalid which PR you're picking up.** Don't assume.
6. **Verify the SQL migration state** by running `\d calendar_entries` and `\d profiles` in Supabase SQL Editor — make sure you're reading current schema.
7. **For coordination with another active dev:** Pick a different file/scope. Backend+SQL on one dev, frontend/UI on another. Avoid both editing `App.jsx` simultaneously.

---

## 11. COORDINATION RULES IF MULTIPLE DEVS ARE ACTIVE

**Two-dev maximum recommended.** Three+ creates merge conflicts faster than features ship.

**File partitioning:**
- Dev A handles "data layer" tasks: Supabase queries, RLS, migrations, API integration, storage uploads.
- Dev B handles "UI layer" tasks: components, styling, layout, mobile rendering.
- The same `src/App.jsx` file gets edited by both eventually, but at different sections.

**Branch strategy:**
- Each dev works on their own branch named `claude/sprint-N-pr-P-{theme}-{dev-letter}`.
- Both target `main`.
- Whichever PR opens first merges first; the other rebases on top.

**Communication:**
- Khalid pastes status updates between the two architect chats and two implementor chats.
- Conflicts get resolved by the architect (this chat), not by either dev independently.

**Stop-the-line conditions:**
- Either dev makes a schema change → both pause until migration is run and confirmed.
- Either dev refactors a shared utility → other dev stops and rebases.

---

## 12. EMERGENCY RECOVERY (if architect chat hits limit)

If you're a fresh Claude instance reading this because the previous architect chat ended:

1. **You are now the architect.** Same role. Read this doc end to end.
2. **Khalid will tell you the current sprint and PR status.** Trust him; he's been in every conversation.
3. **The implementor (Claude Code) is trustworthy** — Sprint 1 shipped clean. Don't second-guess working code.
4. **Defer to Khalid on strategy decisions** until you've caught up. Strategic context lives in this doc + his head + the chat history he can paste.
5. **Don't rebuild what's been decided.** Section 8 is the decisions log — these are settled.
6. **Keep the architect-implementor split.** You spec, Claude Code builds. Don't write production code yourself.

---

## 13. KHALID'S COMMUNICATION PREFERENCES

- Lean responses, no essays
- Notes format > paragraph format for status updates
- Skip obvious steps ("log into GitHub")
- Speak with confidence in him as a founder
- Token-conscious — don't pad
- Calls Claude "Claudisson" or "Claudesson" — affectionate, not formal
- Honesty > performance. He'd rather hear "this is hard, here's why" than "yes I can do that easily" followed by failure.

---

## 14. WHAT NOT TO DO

- Don't promise the marketplace will ship faster than 6+ months from now.
- Don't agree to build tattoo-specific booking SaaS. The wedge is the Tattoo *Package* (templates + tuned blocks), not full Vagaro replacement.
- Don't add email/calendar/SMS integrations (out of scope for the platform thesis).
- Don't suggest Tailwind, Material UI, or any UI framework. Plain CSS only.
- Don't reframe Koy as a productivity tool, Linktree alternative, or Notion competitor. None of those are accurate.
- Don't recommend going viral via paid ads — Koy's growth model is page-as-marketing organic loop, not paid acquisition.
- Don't suggest payment integrations before Sprint 6 (Stripe Connect for marketplace). Premium subscription Stripe is fine earlier as a separate concern.

---

**End of handoff doc.**

If you're a developer or future Claude instance reading this and something is unclear, ask Khalid. He has more context than this document captures.
